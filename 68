#include <stdio.h>

int main() {
    int n, i, x = 0;
    int arr[1000];

    scanf("%d", &n);
    for(i = 0; i < n - 1; i++) {
        scanf("%d", &arr[i]);
        x ^= arr[i];
    }

    for(i = 0; i < n; i++)
        x ^= i;

    printf("%d", x);

    return 0;
}
