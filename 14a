#include <stdio.h>

int main(){
    int a,sum=0;
    printf("Enter the nth tearm\n");
    scanf("%d",&a);
    for(int i =1;i<=a;i+=2)
    {
        sum+=i;
    }

    printf("Sum is: %d",sum);
    return 0;
}
