#include <stdio.h>

int main() {
    int n, i;
    int nums[1000], left[1000], right[1000], ans[1000];

    scanf("%d", &n);
    for(i = 0; i < n; i++)
        scanf("%d", &nums[i]);

    left[0] = 1;
    for(i = 1; i < n; i++)
        left[i] = left[i - 1] * nums[i - 1];

    right[n - 1] = 1;
    for(i = n - 2; i >= 0; i--)
        right[i] = right[i + 1] * nums[i + 1];

    for(i = 0; i < n; i++)
        ans[i] = left[i] * right[i];

    for(i = 0; i < n; i++)
        printf("%d ", ans[i]);

    return 0;
}

