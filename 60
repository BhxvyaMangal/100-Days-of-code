#include <stdio.h>

int main() {
    int n, k, i, j;
    int arr[1000];

    scanf("%d", &n);
    for(i = 0; i < n; i++)
        scanf("%d", &arr[i]);
    scanf("%d", &k);

    for(i = 0; i <= n - k; i++) {
        int max = arr[i];
        for(j = i + 1; j < i + k; j++)
            if(arr[j] > max)
                max = arr[j];
        printf("%d ", max);
    }

    return 0;
}

