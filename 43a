#include <stdio.h>

int main() {
    char str[1000], rev[1000];
    int i = 0, j;

    fgets(str, 1000, stdin);

    while(str[i] != '\0')
        i++;

    j = 0;
    for(int k = i - 1; k >= 0; k--)
        rev[j++] = str[k];

    rev[j] = '\0';

    printf("%s", rev);

    return 0;
}
